/**
* @brief Compute length of string s.
* @param s Pointer to string.
* @return Length of string (not including terminating '\0').
* @example int len = str_len("Hello"); // len == 5
*/
int mystrlen(char *s)
{
	int i=0;
	for(;s[i]!='\0';i++){
		;
}
	return i;
}
